#include <stdio.h>
/*Exercise 1-7. Write a program to print the value of EOF.*/
int main(){
	printf("the value of EOF(%%d): %d\n", EOF);
	printf("the value of EOF(%%x): %x\n", EOF);
	printf("the value of EOF(%%f): %f\n", EOF);
	printf("the value of EOF(%%o): %o\n", EOF);
}
